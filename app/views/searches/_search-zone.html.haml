.drop_downs
  %h3.large-word 詳細検索

  = search_form_for@search,url: searches_path do |f|
    .form_normal
      %i.fas.fa-plus
      キーワードを追加する
      = f.search_field :name_cont_any, class: "input-default", placeholder: "#{@items.name}"
    .form__slot
      %i.fas.fa-coins
      = f.label :karioki, "価格", class: "label-font"
      = f.search_field :price_gteq,class: 'search__box-half', placeholder: "¥ Min"
      %span ~
      = f.search_field :price_lteq,class: 'search__box-half', placeholder: "¥ Max"
    .form__slot
      - rank = {全部: 0, 新品、未使用: 1, 未使用に近い: 2, 目立った傷や汚れなし: 3, やや汚れや傷あり: 4, 傷や汚れあり: 5, 全体的に状態が悪い: 6}
      %i.fas.fa-star
      = f.label :condition_in, '商品の状態', class: "label-font"
      %p
      - rank.each do |key, value|
        .checkbox_default
          = f.check_box :karioki, {checked: false, multiple: true, class: "checkbox_space"}
          = key
    .form__slot
    .button-box
      = f.button "クリア", type: :reset, class: "button-clear"
      = f.submit "完了", class: "button-submit"


