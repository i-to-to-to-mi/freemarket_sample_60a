.wrapper
  .profile-register
    .profile-register__heading
      = render "register-head"
      .signup-bar
        %ul.progressbar
          %li.active
            会員情報
          %li.active
            電話番号認証
          %li
            お届け先住所入力
          %li
            支払い方法
          %li
            完了
    .profile-register__main
      %h2.profile-register__main--head
        電話番号認証
      .profile-register__main--form
        = form_for("#") do |f|
          .profile-register__main--head
            .profile-register__main--form__contents
              .profile-register__main--form__contents--form
                .profile-register__main--form__contents--form__word
                  認証番号 
                = f.text_field :nickname, class: "input-default", placeholder: "認証番号を入力"
                %p SMSで届いた認証番号を入力してください
              = f.submit "認証して完了", class: "default-btn"
          .profile-register__main--head
            .profile-register__main--form__contents
              .profile-register__main--form__contents--form
                .profile-register__main--form__contents--form__word
              %h3 30秒たっても認証番号が届かない方へ
              %p 電話で認証番号を聞くことができます
              = link_to root_path, class: "default-btn" do
                電話で認証番号を聞く（通話料無料）
          .profile-register__main--form__sending
            %h3 認証番号を再送することもできます。もう一度電話番号を入力して下さい。
            = link_to root_path do
              電話番号を再度入力する
            .profile-register__main--form__sending--top
              %span ※SMSが届かない場合は
              = link_to root_path do
                SMS受信設定
              %span を確認して、もう一度電話番号を入力してください。
      .profile-register__footer
        = render "register-footer"

